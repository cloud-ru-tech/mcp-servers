[tool:pytest]
minversion = 7.0
addopts = -ra -q --strict-markers --strict-config
testpaths = test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: интеграционные тесты (требуют интернет)
    slow: медленные тесты
asyncio_mode = auto 